url: https://gaborcsardi.github.io/tsjsonc/

template:
  package: tidytemplate
  bootstrap: 5

development:
  mode: auto

reference:
- title: Quickstart
  desc: |
    * See [quickstart](quickstart.html) for a quick demonstration of tsjsonc.
    * See `serialize_json()` to create JSON from R.
    * See `unserialize_json()` to parse JSON into R.
    * See `ts_parse_jsonc()`, `select()`, and `unserialize_selected()` to parse
      parts of JSON into R.
    * See `ts_parse_jsonc()`, `select()`, `format_selected()`, and `save_json()`
      to format parts of a JSON file or string and save it to a file.
    * See `ts_parse_jsonc()`, `select()`, `delete_selected()`,
      `insert_into_selected()`, `update_selected()`, and `save_json()` to
      edit a JSON file or string and save it to a file.

- title: Serialization
- contents:
  - serialize_json
  - unserialize_json

- title: Printing
- contents:
  - format.tsjsonc
  - print.tsjsonc

- title: Extraction
- contents:
  - ts_parse_jsonc
  - select
  - select_query

- title: Manipulation
- contents:
  - delete_selected
  - deleted
  - format_selected
  - insert_into_selected
  - save_json
  - unserialize_selected
  - update_selected
  - "[[.tsjsonc"
  - "[[<-.tsjsonc"

- title: Utilities and Options
- contents:
  - query_json
  - syntax_tree_json
  - token_table
  - tsjsonc_options
  - "[.tsjsonc"

- title: internal
  contents:
  - quickstart
