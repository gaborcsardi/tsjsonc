url: https://gaborcsardi.github.io/tsjsonc/

template:
  package: tidytemplate
  bootstrap: 5

development:
  mode: auto

reference:
- title: Quickstart
  desc: |
    * See [quickstart](quickstart.html) for a quick demonstration of tsjsonc.
    * See `ts_serialize_jsonc()` to create JSON from R.
    * See `ts_unserialize_jsonc()` to parse JSON into R.
    * See `ts_parse_jsonc()`, <code>[ts_tree_select()](
      ts_tree_select.ts_tree_jsonc.html)</code>, and
      `ts_tree_unserialize()` to parse parts of JSON into R.
    * See `ts_parse_jsonc()`, `ts_tree_select()`, `ts_tree_format()`,
      and `ts_save_json()` to format parts of a JSON file or string and
      save it to a file.
    * See `ts_parse_jsonc()`, `ts_read_jsonc()`, `ts_tree_select()`,
      `ts_tree_delete()`, `ts_tree_insert()`, `ts_tree_update()`, and
      `ts_tree_write()` to edit a JSON file or string and save it to a file.

- title: Parsing
  desc: |
     <dl>
     <dt><code>[ts_tree_new()](ts_tree_new.ts_language_jsonc.html)</code></dt>
     <dd>Create a new tree-sitter tree for a JSONC document.</dd>
     </dl>
- contents:
  - ts_parse_jsonc
  - ts_read_jsonc
  - ts_language_jsonc

- title: Selection
  desc: |
     <dl><dt><code>[ts_tree_select()](ts_tree_select.ts_tree_jsonc.html)</code></dt>
     <dd>Select parts of a JSONC tree-sitter tree.</dd></dl>

- title: Editing
  desc: |
     <dl>
     <dt><code>[ts_tree_delete()](ts_tree_delete.ts_tree_jsonc.html)</code></dt>
     <dd>Delete selected elements from a JSONC tree-sitter tree.</dd>
     </dl>

- title: Serialization
- contents:
  - ts_serialize_jsonc
  - ts_unserialize_jsonc

- title: Formatting
- contents:
  - ts_format_jsonc

- title: Options
- contents:
  - tsjsonc_options

- title: internal
  contents:
  - quickstart
  - ts_tree_delete.ts_tree_jsonc
  - ts_tree_new.ts_language_jsonc
  - ts_tree_query.ts_tree_jsonc
  - ts_tree_select.ts_tree_jsonc
