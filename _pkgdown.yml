url: https://gaborcsardi.github.io/tsjson/

template:
  package: tidytemplate
  bootstrap: 5

development:
  mode: auto

reference:
- title: Quickstart
  desc: |
    * See [quickstart](quickstart.html) for a quick demonstration of tsjson.
    * See `serialize_json()` to create JSON from R.
    * See `unserialize_json()` to parse JSON into R.
    * See `ts_tree_read_jsonc()`, `select()`, and `unserialize_selected()` to parse
      parts of JSON into R.
    * See `ts_tree_read_jsonc()`, `select()`, `format_selected()`, and `save_json()`
      to format parts of a JSON file or string and save it to a file.
    * See `ts_tree_read_jsonc()`, `select()`, `delete_selected()`,
      `insert_into_selected()`, `update_selected()`, and `save_json()` to
      edit a JSON file or string and save it to a file.

- title: Serialization
- contents:
  - serialize_json
  - unserialize_json

- title: Printing
- contents:
  - format.tsjson
  - print.tsjson

- title: Extraction
- contents:
  - ts_tree_read_jsonc
  - select
  - select_query

- title: Manipulation
- contents:
  - delete_selected
  - deleted
  - format_selected
  - insert_into_selected
  - save_json
  - unserialize_selected
  - update_selected
  - "[[.tsjson"
  - "[[<-.tsjson"

- title: Utilities and Options
- contents:
  - query_json
  - syntax_tree_json
  - token_table
  - tsjson_options
  - "[.tsjson"

- title: internal
  contents:
  - quickstart
